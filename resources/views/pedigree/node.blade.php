<div class="node"><div class="card">@if($node)<strong>{{ $node->ring ?: 'Fără serie' }}</strong><span>{{ $node->name }}</span><span class="muted">{{ $node->color }}</span>@else<span class="muted">Necunoscut</span>@endif</div>@if($remaining>0)<div class="parents">@include('pedigree.node',['node'=>$node?->father,'remaining'=>$remaining-1])@include('pedigree.node',['node'=>$node?->mother,'remaining'=>$remaining-1])</div>@endif</div>